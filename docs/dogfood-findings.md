# Dogfood Findings

Cursor evaluation log for graphirm agent sessions. One row per completed task.

| Date | Session ID | Task | Grade | Finding |
|------|-----------|------|-------|---------|
| 2026-03-20 | 6cb219fe | message-count-field | partial | Struct field correct; session failed mid-run — agent used `cd` in bash (state not persisted) and was misled by Cursor worktree paths in ls output |
| 2026-03-20 | 94ce01fd | tool-call-count-field | partial | All code correct and compiles; session failed at context overflow (273k > 262k) — agent read full routes.rs (2000+ lines) multiple times instead of grep → targeted read |
| 2026-03-20 | 0b1fde7d | health-session-count | partial | All code correct and compiles (incl. test update); context overflow again — Qwen ignores grep-first system prompt instruction; fix is to provide exact line numbers in task prompt |
| 2026-03-20 | db0590a9 | health-session-count-v2 | pass | First full pass — agent used grep→read(offset+limit) pattern, 11 tool calls, no context overflow, compiled + tests passed. Key fix: documented offset/limit in system prompt tool description |
| 2026-03-20 | 5b997cfb | planning-layer-project-mode | partial | All 4 actions implemented correctly (create/list/link_session/update), graph model understood well. Failed on Rust dep management (added petgraph 0.6 when graph crate re-exports 0.7) and ownership in spawn_blocking closures. No tests written — context exhausted on compile error loop. Fix: document re-exports in system prompt + add spawn_blocking clone pattern |
| 2026-03-20 | f7b269d3 | project-mode-tests | pass | All 5 tests written correctly (create, create_with_parent, list, link_session, update). Used graphirm_graph re-exports — no petgraph/chrono added. Proper Arc cloning in spawn_blocking. Fixed String borrow issues independently. 18 tool calls. System prompt crate dep + async pattern sections validated |
| 2026-03-20 | 8de099f4 | cargo-check-tool | partial | Tool structure excellent — correct trait impl, registration, 9 tests, clippy/fmt clean, used re-exports. Two critical bugs: (1) early return on non-zero exit code meant errors never parsed; (2) JSON deserialization expected top-level fields but cargo nests diagnostics inside `message` object. Tests only covered clean path. 43 tool calls. Agent self-fixed clippy/fmt issues. Fix: need to teach cargo JSON envelope format |
| 2026-03-20 | fca4e36b | grep-context-lines | hung | Agent read grep.rs, wrote correct context_lines param + execute logic, then LLM call hung forever (Qwen/OpenRouter). Session stayed "running" indefinitely — exposed missing LLM timeout. Second session (2513f46b) same result. Bug: `timeout_seconds` was dead config, never wired to LLM call. Fixed by adding `tokio::time::sleep` arm to `tokio::select!` in workflow.rs |
| 2026-03-20 | e46dba9c | bfs-max-depth | fail | Model ID bug — `openrouter/qwen/qwen3-coder-next` sent as-is to OpenRouter API (prefix not stripped). Cause: explicit `model` in create_session curl bypassed server's default (which strips prefix). Fix: omit model field in API call, use server default |
| 2026-03-20 | 6a31b075 | bfs-max-depth-v1 | partial | Correct intent, wrong abstraction. Agent rewrote BFS with raw SQL in tools crate — accessed private `graph.pool` field and used `rusqlite` (not a dep of graphirm-tools). 2 compile errors. Never ran cargo_check — session died on rig JSON parse error before reaching verification. Good test quality (3 cases). Fix: added "Abstraction boundaries" section to system prompt |
| 2026-03-20 | b518e3b3 | bfs-max-depth-v2 | pass-with-assist | Abstraction boundary prompt fix worked. Agent modified `traverse()` in graphirm-graph to return `Vec<(GraphNode, usize)>`, updated tool to use public API, added `max_depth` param + `[depth=N]` output. cargo_check passed, 20/20 tools tests passed. Died on rig JSON parse error before fixing graph crate tests (2-line `.0` accessor fix done by Cursor). 90% agent, 10% human |
| 2026-03-20 | 881c2a1f | graph-query-neighbors | pass-with-assist | Agent added `neighbors` mode: description, params (direction, edge_types), mode dispatch, execute_neighbors fn, 3 tests. 5 edit calls, 22 messages total. Every edit call hung on `rg` in impact analysis (missing path arg → stdin read). Context overflow (266k > 262k) killed session before cargo_check. Human fixes: (1) rewrote execute_neighbors — agent stored borrowed refs across scope, redundant queries, format string error; (2) fixed `crate::nodes::` → `graphirm_graph::nodes::` in tests; (3) capitalization in test assertion. Also discovered+fixed impact provider `rg` bug. ~65% agent, ~35% human |
| 2026-03-20 | 87d51c4b | graph-query-stats | near-pass | Agent added `stats` mode: description, params, dispatch, execute_stats fn, 2 tests. 6 edits, 40 messages. `rg` fix confirmed working — no hangs. Ran cargo_check (pass!), ran tests, diagnosed pre-existing test context nodes, fixed test. Context overflow (268k) on final turn. Human: 2 clippy fixes (`format!("literal")` → `.to_string()`), added "stats" to read-only modes list. ~95% agent, ~5% human |
| 2026-03-21 | 17c7401c | lesson-briefing | partial | `build_lessons_summary` correct — two `search_knowledge` calls, merge, format with `[lesson]`/`[convention]` tags, wired into `build_repo_briefing`, 3 tests. Sort bug: used `metadata.get("created_at")` but `created_at` is top-level `DateTime<Utc>` on `GraphNode` — sort was no-op. 1-line fix by reviewer. 31 tool calls, 64 messages, no context overflow. ~95% agent, ~5% human |
| 2026-03-21 | e7746549 | max-output-tokens | pass | Added `max_output_tokens: Option<u32>` to AgentConfig (4 locations), wired fallback chain in workflow.rs, set 1500 in default.toml, updated test. Zero bugs — first clean pass with no reviewer fixes. 28 messages. 100% agent |
| 2026-03-21 | 5e1bf77c | read-auto-truncate | pass | `MAX_AUTO_LINES=300` const, boolean detection flags for offset/limit, truncation logic + notice. 2 new tests. Self-fixed compile error (`.is_some()` on `Value` → `.as_u64().is_some()`). Also ran `cargo fmt` globally (reformatted unrelated `graph_query.rs` — reverted). Reviewer added "Use offset and limit" to notice text. ~98% agent |
| 2026-03-21 | 86c5cc2c | design-system-theme | pass | Design tokens (spacing/typography/surfaces), light/dark theme via useTheme hook (localStorage + system preference), EDGE_COLORS DRYed to CSS variable lookup with theme-aware cache. 28 messages, clean build, committed in one shot. 100% agent |
| 2026-03-21 | 7ef9d015 | auto-compaction-trigger | partial | select_nodes_for_compaction + compaction_threshold field correct. Signature change (Arc vs &dyn LlmProvider) cascaded errors into coordinator.rs, multi.rs, workflow.rs tests. Test bugs: Arc::new(provider) in existing tests, Utc/AgentData missing imports, token threshold math wrong (140 < 160). Session failed at 68 messages. Cursor fixed: reverted signatures, fixed imports, fixed math, fixed clippy. 60% agent, 40% human. New rule needed: grep all call sites before changing a public signature. |
| 2026-03-21 | 54d3aa86 | fix-dogfood-system-prompt | pass | Added caller inventory rule to system_prompt in dogfood SKILL.md. Grep to locate, targeted edit, re-read to verify. Clean execution, nothing else touched. 100% agent |
| 2026-03-21 | e1c921f2 | sqlite-indices | pass | 4 indices added to init_schema(): idx_nodes_created_at, idx_edges_created_at, idx_nodes_session_id (json_extract), idx_nodes_type_created composite. CREATE INDEX IF NOT EXISTS — safe for existing DBs. 71 tests pass. Additive-only, minimal reads. 100% agent |
| 2026-03-21 | be8ca2d3 | node-cache | pass | TTL cache (60s) for get_node: node_cache field, Instant import, cache check/populate/invalidate. let-chain && for clippy. LockPoisoned on all lock errors. Both open() and open_memory() initialized. 71 tests pass, clippy clean. Session died to context overflow on final whole-file read (not a logic failure). 100% agent |
| 2026-03-22 | ff7287ea | cursor-import | partial | Parser + write_transcript + 8 tests all correct. Sampled real transcripts to validate format — good discipline. Session failed before CLI Task 3 (context overflow). One edge case missed: blank lines after </user_query> collected into content ("Hello\n\n" vs "Hello\n") — fixed manually. CLI, docs, and commit completed by Cursor. ~70% agent, ~30% human. |
| 2026-03-22 | c5393c88 | perf-remaining | pass | Task 1 (SQL LIMIT fast path): correct, clean, tests pass. Task 2 (agent cache): code correct, session failed before commit. One collapsible-if clippy issue fixed manually (nested if let → let-chain &&). ~95% agent. |
| 2026-03-22 | a29c5f96 | pinned-knowledge-store | pass | list_pinned_knowledge method + 3 tests in GraphStore. 5 tool calls, extremely efficient (1 read, 2 edits, 1 test, 1 clippy). Zero bugs. 100% agent |
| 2026-03-22 | 5076916d | pinned-briefing | pass | build_pinned_summary + wire into build_repo_briefing + 3 tests. 8 tool calls, all correct. Follows existing codebase patterns. 100% agent |
| 2026-03-22 | e6dc2e51 | pinned-api-endpoint | pass | POST /api/knowledge endpoint + CreateKnowledgeRequest type + deserialization test. 15 tool calls. Minor .map_err indentation issue fixed by reviewer. ~98% agent |
| 2026-03-22 | 040a75fe | get-pinned-knowledge-endpoint | pass | E2E validation of pinned Knowledge feature. GET /api/knowledge/pinned endpoint + PinnedKnowledgeQuery + test. All 5 pinned conventions surfaced in repo briefing and followed. Self-fixed unused import. 25 tool calls. 100% agent |
| 2026-03-22 | 714dd03a | knowledge-cli-subcommand | pass | `graphirm knowledge list/pin/unpin` CLI. KnowledgeAction enum + Commands variant + match arm. Follows ImportCursor pattern exactly. Re-exports, no unwrap, ? throughout. 84 lines, 1 file. 100% agent |
| 2026-03-22 | 2e5c6fd4 | model-router-types | fail | Session timed out. Agent added pub mod + re-export to lib.rs correctly but failed before creating router.rs. 7 messages, 3 tool calls. Wrote lib.rs edits but never the main file. ~10% agent (lib.rs edits reused) |
| 2026-03-22 | 4728ef95 | model-router-config | pass | Wire ModelRoutingConfig into AgentConfig. Import, struct field, Default impl, AgentConfigSection, from_toml wiring, 2 tests. All correct. ~5 min runtime, ~15 tool calls. 100% agent |
| 2026-03-22 | 92ebc97b | model-router-workflow | pass | Wire router into stream_and_record. TurnSignals extraction in spawn_blocking, router selection with tracing, metadata stamping. Clean diff, no lint issues. ~2 min runtime. 100% agent |
| 2026-03-22 | ndstrms:01e6ec9b | smoke-test | pass | First cross-project dogfood. Graphirm on Nodestradamus100 machine (91.98.94.217). Read BACKLOG.md, ran pytest (self-corrected on 120s timeout), identified 5 placeholder gates + 13/14 tests passing. 5 tool calls. Pipeline validated end-to-end. |
| 2026-03-22 | ndstrms:5c8c80a6 | validation-gates-3 | B+ | All 3 remaining gates (ImportDetectionRecallGate, KeywordExtractionGate, CrossRepoStabilityGate) implemented, wired into EnhancedValidationRunner, tests added. 95 tests pass. 148 messages (~74 turns). Minor issues: (1) keywords.json uses `import` key but gate checks `import_keywords` — mismatch would cause real runs to report incomplete categories; (2) CrossRepoStabilityGate has duplicated stability_ratio calculation; (3) bottom-of-file try/except ImportError for ImportDetector is fragile. Session ended on `Tool not found: submit` — Qwen model quirk. Root cause of prior 4 session failures was 50-turn limit (not model errors) — fixed by increasing max_turns to 100 in config/default.toml. ~100% agent |
| 2026-03-22 | ndstrms:n/a | validation-gates-audit | n/a | Discovered that BaselineCorrelationGate and WithinFileSimilarityRatioGate were already implemented with tests in commit a0cced1 (March 19) — BACKLOG was stale saying "2 remaining". All 8/8 gates are real. Updated BACKLOG.md accordingly. No agent session needed. |
| 2026-03-22 | ndstrms:f7907bae | layer-integration-verify | pass | Qwen on OpenRouter. Audited pipeline + fixed `Layer1Orchestrator.add_parsed_edges` (dict metadata vs wrong list extractor → KeyError risk). Added `tests/test_layer_integration.py` (13 pass) + `docs/layer-integration-verification.md`. Verified on host: `pytest tests/test_layer_integration.py`. Session completed without `submit` tool error. Committed+pushed nodestradamus100. |
| 2026-03-22 | ndstrms:624af505 | layer2-embedding-dataflow | pass | Qwen. Added `tests/test_layer2/test_embedding_to_edge_flow.py` (4 tests): identical vectors→edges, orthogonal→no edges, compute_embeddings called, compute_embedding_similarity called. Session status "failed" (likely Qwen submit tool quirk) but all tests pass. Committed 6b63d93 on ndstrms. |
| 2026-03-22 | ndstrms:dbf3977b | repo-processing-depth | pass | P1. Added `tests/test_repo_processing_depth.py` (5 tests): assert CHUNK nodes and code-level edges (TFIDF_SIMILAR/SEMANTIC_SIMILAR/REFERENCE) exist after full pipeline. Session status "failed" (agent completed work, all tests pass). Committed 75b2e11 on ndstrms. |
| 2026-03-22 | ndstrms:a8ae3e58 | quality-metrics-repo-processing | pass | P1. Added quality_metrics to TreeMetricsCalculator: code_level_edge_count, structural_edge_count, chunks_per_file, quality_level (superficial/structural_only/meaningful). test_quality_metrics_present_and_classified asserts full-pipeline metrics. 6 tests pass. Committed b17ec10 on ndstrms. |
| 2026-03-22 | ndstrms:423f39c0 | v2-insights-extractor | pass | Phase 1 v2 insight layer (plan). `src/nodestradamus/insights/` RepoInsightExtractor: hotspots (PageRank), cycles (SCC), duplicate_clusters (similarity), dead_code_candidates, coupling. `tests/test_insights/test_extractor.py` 6 passed. Commit 756c2aa; session completed; evaluation sent. |
| 2026-03-22 | ndstrms:c376f813 | v2-insights-persistence | pass | `persistence.py`: `graph_to_serializable`, `save_repo_insights` → `graph.json`, `metrics.json`, `insights.json`, `summary.md`. `tests/test_insights/test_persistence.py`; `pytest tests/test_insights/` 16 passed. Commit a85f60c pushed. Session status `failed` on empty final assistant turn after successful push; evaluation sent. |
| 2026-03-22 | ndstrms:9c91b6e3 | v2-insights-batch-cli | pass | Phase 2: `pipeline/full_graph.py` `run_full_graph_pipeline`, `insights/batch.py`, CLI `nodestradamus insights batch-cache`, `test_batch.py`, test_extractor refactor. `pytest tests/test_insights/` 19 pass; `test_repo_processing_depth` 6 pass. Commit c63033a pushed; evaluation sent. |
| 2026-03-22 | ndstrms:manual | phase2-batch-run | partial | Ran batch on 25 real repos (1–20 MB on disk). **19 ok, 6 failed** (d3, axios, lodash — Mistral embedding 8192 token limit). Key finding: **all 19 successful repos have zero CALLS/REFERENCE/INHERITS edges** — only semantic_similar/contains/next. Hotspots, cycles, coupling, dead_code all empty. Root cause: Layer 3 enriches only 100 candidates; `max_files_to_parse` + `min_confidence_for_parsing` cap prevents tree-sitter from firing broadly enough. Duplicate_clusters work (2-3 per repo) but include `.git/hooks` noise. Phase 3 blocked until Layer 3 edge coverage is fixed. |
| 2026-03-23 | ndstrms:57c769aa | fix-layer3-edge-coverage | pass | Root cause was chunk→file ID mismatch: `layer25_metadata` keyed by CHUNK IDs, but Layer 3 expects FILE IDs (CHUNK nodes have `file_path` attr, Layer 3 looks for `path`). Agent fixed 5 files: added `parent_file_id` to candidates, remapped metadata with dedup, lowered `min_confidence_for_parsing` 0.7→0.3, added Layer 3 logging, enhanced test fixtures with cross-file imports + REFERENCE assertion. 25 tests pass. Verified: REFERENCE=5, DEFINES=7 edges on test repo; hotspots/cycles/dead_code/coupling all populated. Commit bc01de7. 100% agent. |
| 2026-03-23 | ndstrms:42a0466e | pipeline-fixes-3 | pass | 3 fixes in one session: (1) `.git/` exclusion — `dirs[:]=` filter in `walk()` + path-parts check in `_should_skip_file`; (2) cache dedup — `iter_cached_repo_entries` uses `seen` dict by `repo_path`, keeps latest `last_accessed`; (3) embedding truncation — char_limit 4x→3x tokens, `MAX_TEXT_LENGTH` 10000→24000. 98 tests pass; 6 pre-existing API-key failures unrelated. Commit 59b24a6. 100% agent. |

| 2026-03-23 | ndstrms:manual | fix-file-tree-skip | pass | False-positive `.cache` path in `_should_skip_file` caused zero FILE/CHUNK nodes across all 30 batch repos. `parts` check removed (redundant with `dirs[:]=` filter). Commit 481bc55. Re-batch in progress. |
| 2026-03-23 | ndstrms:0a40864e | perf-optimizations | pass | All 3 tasks in one session: (1) sort repos small-to-large via `du -sb` in `iter_cached_repo_entries`; (2) parallel chunking — ThreadPoolExecutor Phase 1 Rust reads + serial Phase 2 graph writes; (3) TokenRateLimiter sliding 60s window + ThreadPoolExecutor(10) for concurrent embedding batches + `tokens_per_minute` in EmbeddingConfig. Self-corrected failed edit, correctly skipped pre-existing test failures. 4 files, 213 lines. Commit 646b838. 100% agent. |
| 2026-03-23 | ndstrms:c0d25b84 | embed-token-trim-retry | B+ | Fix: catch-and-trim retry on Mistral HTTP 400 / code 3210 (token limit). In `_embed_one_batch`: detect error pattern, parse offending input index via regex, halve that text, `continue` without incrementing retry counter (cap at 3 trims). Implementation correct. Test written with right mock pattern but one bad assertion (`len(texts[1]) < len(original_text)` checks caller list, not internal batch copy) — fixed by Cursor. 12 tests pass, 6 pre-existing failures unchanged. Commit c3bc394. |

## Open investigations

- ~~**Qwen submit-tool quirk:**~~ **Resolved.** Added no-op `submit` tool (`crates/tools/src/submit.rs`) — models that call it now get success instead of "Tool not found", so sessions no longer end in "failed" status.
- ~~**Layer 3 edge coverage gap (ndstrms):**~~ **Resolved.** Root cause: `layer25_metadata` was keyed by CHUNK node IDs (from `add_parser_selection_metadata`), but Layer 3's `_parse_file` expected FILE node IDs. CHUNK nodes have `file_path` attribute, Layer 3 looked for `path` → got None → skipped all files. Fix: added `parent_file_id` to candidates, remapped in `full_graph.py` with file-level deduplication. Secondary fix: lowered `min_confidence_for_parsing` from 0.7 to 0.3. Commit bc01de7.
- ~~**Mistral embedding token limit (ndstrms):**~~ **Resolved.** `char_limit` changed from `MAX_TOKENS * 4` to `MAX_TOKENS * 3` (more conservative for minified code); `MAX_TEXT_LENGTH` updated to 24000. Commit 59b24a6.
- ~~**Cache deduplication (ndstrms):**~~ **Resolved.** `iter_cached_repo_entries` now deduplicates by `repo_path`, keeping the entry with the latest `last_accessed`. Commit 59b24a6.
- ~~**`.git/` chunking (ndstrms):**~~ **Resolved.** `dirs[:]=` filter in `FileTreeWalker.walk()` prunes `.git` (and `__pycache__`, `node_modules`, `vendor`) before descending; `_should_skip_file` also checks path parts for hidden dirs. Commit 59b24a6.
- **Pre-existing test_backend.py failures:** 6 tests in `tests/test_embedding/test_backend.py` fail due to hardcoded error message regex and empty-API-key behavior — pre-existing, not caused by recent changes. Should be fixed to prevent masking regressions.
- ~~**Embedding 400 token-limit retry loop (ndstrms):**~~ **Resolved.** Previous retry loop resent the same oversized payload on every attempt — 3 identical 400s, then whole repo failed. Fix: catch-and-trim in `_embed_one_batch` — detect `"Input id N has X tokens, exceeding max 8192 tokens"`, parse index, halve text, `continue` without counting as a retry. Cap trim retries at 3. Commit c3bc394.
- ~~**False-positive hidden-dir skip in `_should_skip_file` (ndstrms):**~~ **Resolved.** The `pipeline-fixes-3` parts-check (`any(part.startswith(".") for part in Path(file_path).parts)`) matched `.cache` in the absolute cache path `/root/project/.cache/repos/...`, causing every file in every cached repo to be skipped → zero FILE nodes → zero CHUNKs → all downstream layers no-ops. Fixed by removing the redundant parts-check (the `dirs[:]=` filter in `walk()` already prevents descent into hidden dirs). Commit 481bc55.
- **Shared EmbeddingCache across batch repos (backlog):** Each `run_full_graph_pipeline` call creates a fresh `EmbeddingCache`; a single shared instance passed to the batch loop would avoid re-embedding identical chunks across repos. Added to ndstrms `BACKLOG.md` item 10 (ff44fb7).
- **Missing tree-sitter parsers (backlog):** `tree_sitter_ruby/rust/c/cpp` not installed — Layer 3 silently skips these files, no REFERENCE/CALLS/INHERITS edges for Ruby, Rust, C/C++ repos. Added to ndstrms BACKLOG item 13 (e6dc059).
- **Per-layer pipeline metrics (backlog):** `metrics.json` only captures Layer 1 structural data; Layers 1.5/2/2.5/3 produce no observable output (edge counts by type, embedding volume, parse success rate, per-layer duration). Added to ndstrms BACKLOG item 14 (e6dc059).
- **Parallel chunking + adaptive token-budget scheduler (backlog):** `_chunk_all_files` is single-threaded on an 8-core machine (~8x speedup with `ProcessPoolExecutor`, item 11). Mistral embedding calls replaced with sliding-window `TokenRateLimiter` driven by `tokens_per_minute = 5_000_000` config (item 12, 2bb0b2f). Limiter fires all batches via `asyncio.gather`, blocks only when 60s window is full. Formula: `max_in_flight ≈ tpm × latency / (60 × tokens_per_batch)` → ~10 concurrent at 5M tpm / 500ms / 4k tokens. Single knob maps directly to Mistral console — upgrade tier by changing one number, no concurrency guessing.
